version: "3.9"

services:
  web:
    build:
      context: ./backend
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    working_dir: /app
    environment:
      DJANGO_DEBUG: "true"
      DB_ENGINE: sqlite3
    volumes:
      - ./backend:/app
